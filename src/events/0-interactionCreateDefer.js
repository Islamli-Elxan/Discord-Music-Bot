/**
 * Runs FIRST for interactionCreate.
 * Play command now defers itself as first line - no early defer needed.
 * Kept for potential future early-defer commands.
 */
module.exports = {
  name: "interactionCreate",
  async execute() {}
};
